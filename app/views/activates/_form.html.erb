<%= form_for(@activate) do |f| %>
  <% if @activate.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@activate.errors.count, "error") %> prohibited this activate from being saved:</h2>

      <ul>
      <% @activate.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :start %><br>
    <%= f.datetime_select :start %>
  </div>
  <div class="field">
    <%= f.label :stop %><br>
    <%= f.datetime_select :stop %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
